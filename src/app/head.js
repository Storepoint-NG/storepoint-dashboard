import { Fragment } from "react";

export default function Head() {
  return (
    <Fragment>
      <link rel="manifest" href="/manifest.json" />
      {/* Add other head tags like meta, title, etc. as needed  */}
    </Fragment>
  );
}
